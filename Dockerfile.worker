FROM node:20-slim

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY tsconfig.json ./
COPY src/ ./src/

# Install tsx for running TypeScript directly
RUN npm install -g tsx

CMD ["tsx", "src/workers/export-worker.ts"]
